# Runtime and reproducibility
seed: 42
device: cuda

# Dataset and trajectory-window sensitivity parameters
data:
  dataset: halfcheetah-medium-v2
  npz_path:
  output_path: data/augmented_srts.npz
  H: 6
  P: 10
  bridge_horizon: 10

# Hierarchical compatibility and filtering sensitivity parameters
stitching:
  top_k: 100
  consistency_threshold: auto
